"use strict"; //treat all JS code as newer version

// alert(3+3)  :- we are using nodejs not browser

/* Always remember code readability should be high & learn 
from the tc39.es & mdn for the better understanding of the js
*/

/* number,bigInt,string,boolean,null(standalone value),undefined(not assign any value),
symbol(unique)
*/
let age=18
console.log(typeof(null))
console.log(typeof(age))
